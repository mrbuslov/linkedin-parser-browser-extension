{
  "name": "linkedin-parser-browser-extension",
  "version": "1.3.4",
  "private": true,
  "description": "Chrome extension that tracks LinkedIn invitations and builds a local contact database.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint linkedin-tracker/**/*.js tests/**/*.js",
    "pack:zip": "node scripts/pack-zip.js"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "globals": "^15.14.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
